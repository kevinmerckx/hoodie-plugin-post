
console.log("back");

module.exports = function(hoodie) {
	console.log('export');
	/*hoodie.task.on('post:add', handle);

	function handle(originDb, post) {
		
	};

	function addMessageCallback(error, post) {
		if(error){
			return hoodie.task.error(originDb, post, error);
		}
		return hoodie.task.success(originDb, post);
	};*/
};

console.log("back");
